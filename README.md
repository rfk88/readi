# Readi

Readi is an AI meeting-prep assistant focused on account executives and job seekers. It aggregates calendar events, emails, Slack history, user-provided context, and trusted public research to deliver actionable briefs before every meeting.

## Current Status
- ✅ Repository scaffolding
- 🚧 Backend, frontend, and AI orchestration TBD

## Development

### Requirements
- Node.js 20+
- npm 10+

### Project Layout (planned)
```
apps/
  backend/   # API, ingestion services, orchestration
  web/       # User-facing dashboard
packages/
  shared/    # Shared utilities, types
```

### Scripts
- `npm run dev:api` – start backend (when implemented)
- `npm run dev:web` – start web client (when implemented)
- `npm run lint` – run workspace linters
- `npm run format` – format with Prettier

## Roadmap
See `/docs/roadmap.md` (coming soon) for milestone details.
